version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend/phishing_detector.db:/app/phishing_detector.db
    environment:
      - PORT=8000
    restart: always
